# Copy to frontend/.env for local dev (Vite reads env from this folder only).
# On Vercel: set the same variable in Project → Settings → Environment Variables, then redeploy.

VITE_API_CORE_URL=http://localhost:4000

# Production example:
# VITE_API_CORE_URL=https://kaliapp-api.onrender.com
